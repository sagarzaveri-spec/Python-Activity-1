def permutations(s,current):
    if len(s)==0:
        print(current)
        return
    for i in range(len(s)):
        remaining=s[:i]+s[i+1:]
        permutations(remaining,current+s[i])
permutations("ABC","")
